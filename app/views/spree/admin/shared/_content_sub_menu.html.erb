<ul class="admin-subnav" data-hook="admin_settings_sub_tabs-1">

  <% if can?(:admin, Spree::Store) %>
    <%= tab :reviews, url: admin_reviews_path %>
  <% end %>

  <% if can?(:admin, Spree::Store) %>
    <%= tab :blog, url: 'blog' %>
  <% end %>
  
  <% if can?(:admin, Spree::Store) %>
    <%= tab :smiles, url: 'smiles' %>
  <% end %>

</ul>
