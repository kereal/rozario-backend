<% admin_breadcrumb(t('spree.content')) %>
<% admin_breadcrumb(link_to plural_resource_name(Spree::Review), spree.admin_reviews_path) %>
<% admin_breadcrumb(t('new_review')) %>


<% content_for :page_actions do %>
<% end %>

<%= render partial: 'spree/shared/error_messages', locals: { target: @review } %>

<%= form_for [:admin, @review] do |review_form| %>
  <%= render partial: 'form', locals: { review_form: review_form } %>

  <div class="clear"></div>

  <%= render partial: 'spree/admin/shared/new_resource_links' %>
<% end %>
